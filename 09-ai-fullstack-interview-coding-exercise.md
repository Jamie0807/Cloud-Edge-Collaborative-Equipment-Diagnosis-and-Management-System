## 1. 业务说明与交付目标

- 系统需要能够在本地和可复现的部署环境中运行，并通过自动化测试、质量门禁和端到端验收。
- 当前没有真实采集装置，使用可配置的程序模拟终端采集；模拟器必须保留与真实设备接入相同的协议边界。
- 页面视觉不是核心目标，但可用性、错误状态、数据可追溯性、安全边界和可维护性属于正式质量要求。
- 建议技术栈：Vue、Spring Boot、MySQL、Python；允许使用 Docker。最终技术决策须记录在项目 spec/plan/ADR 中。

## 2. 总体任务

开发一个可运行的云边协同设备诊断管理系统，完成以下三项任务：

1. 模拟采集装置/感知终端；
2. 实现边端服务器功能；
3. 实现云端数据管理功能。

整体链路如下：

```text
感知终端模拟器
  -> 边端接收和标准化数据
  -> 边端模拟算法分析
  -> 边端缓存并上报诊断结果
  -> 云端保存、查询和展示
```

系统中需要区分以下对象：

| 对象       | 含义                         |
| ---------- | ---------------------------- |
| 被监测设备 | 需要进行状态诊断的电力设备   |
| 感知终端   | 负责采集原始检测数据的装置   |
| 边端节点   | 部署边端服务的站内服务器     |
| 云端系统   | 接收和管理边端上报数据的平台 |

数据中需要分别使用：

- `assetId`：被监测设备编号；
- `terminalId`：感知终端编号；
- `edgeId`：边端节点编号；
- `eventId`：检测事件唯一编号。

## 3. 任务一：模拟采集装置/感知终端

使用 Python 编写感知终端模拟器，至少模拟 3 台终端，可分别代表红外、声纹和局放采集装置。

需要实现：

- 每 10 秒向边端服务发送终端心跳；
- 定时生成对应模态的模拟检测数据：红外每 5 秒、声纹每 20 秒、局放每 60 秒生成一次；
- 使用本地测试图片或文件模拟终端采集到的原始检测数据；
- 将检测文件及其元数据发送给边端服务，不直接发送到云端；
- 终端编号、所属边端、站点、模态和上报周期可以通过配置修改。

终端心跳和检测数据的通信方式由实现方案确定，并将协议、可靠性、幂等性和错误处理规则记录在项目设计文档中。

## 4. 任务二：实现边端服务器功能

边端服务代表部署在站内边端服务器上的实际业务程序。该服务需要实际开发和运行，不是模拟感知终端。

需要实现：

- 接收感知终端心跳、检测文件及其元数据；
- 记录终端编号、模态、最后在线时间和运行状态；
- 保存检测文件，并对元数据进行字段校验和标准化；
- 将边端保存后的文件交给模拟算法进行单项分析；
- 将检测数据和算法结果组合成标准化诊断结果；
- 在本地保存待上报诊断结果；
- 向云端上报边端节点状态和诊断结果。

### 4.1 模拟分析算法

模拟分析算法属于边端服务器功能的一部分，可以实现为边端内部模块，也可以实现为独立的 Python 服务。

算法输入包括被监测设备编号、终端编号、数据模态和边端保存后的文件路径，返回缺陷类型、置信度和严重等级等诊断结果。

不要求运行真实深度学习模型，但需要具备输入校验、异常返回和日志记录。

### 4.2 边端节点状态上报

边端需要定时向云端上报边端编号、所属站点、CPU、内存、GPU/NPU 和上报时间等运行状态。

边端服务可以使用 Python、Java 或其他适合的技术实现，但需要记录选择依据、运行方式和运维边界。

## 5. 任务三：实现云端数据管理

使用 Spring Boot、MySQL 和 Vue 实现云端数据管理功能。

### 5.1 云端后端

需要实现：

- 接收并保存边端节点心跳；
- 管理被监测设备、感知终端和边端节点之间的关系；
- 保存边端节点编号、所属站点、最后在线时间和在线状态；
- 超过 60 秒未收到心跳时，将边端节点判断为离线；
- 接收并保存边端上报的诊断结果；
- 保存模态、缺陷类型、置信度、严重等级、发生时间和证据文件地址；
- 支持按被监测设备、感知终端、边端节点、模态和时间查询；
- 提供清晰的接口返回、异常处理和关键业务日志；
- 至少编写 2 个有效测试。

### 5.2 Web 管理页面

需要实现：

- 边端节点列表：展示边端编号、所属站点、在线状态和最后在线时间；
- 诊断结果列表：展示被监测设备、感知终端、边端节点、模态、缺陷类型、置信度、严重等级和发生时间；
- 诊断详情：展示诊断结果和证据文件地址；
- 支持按被监测设备、边端节点和模态筛选诊断结果。

## 6. 交付内容

- 可运行的感知终端模拟器；
- 可运行的边端服务及模拟分析算法；
- 可运行的云端后端和 Web 前端；
- 数据库表设计和接口说明；
- 启动、部署和联调说明；
- 测试代码及测试结果；
- 记录 AI 辅助研发的产出、人工审查与关键决策，确保变更可追踪、可复核；
- 说明当前未完成内容、风险、限制和后续演进计划。
