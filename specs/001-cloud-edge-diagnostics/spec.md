# 001 云边协同设备诊断管理规格

**状态：** Draft — **阻断：** D-001 至 D-014 未经用户确认，且批准的 monorepo bootstrap 尚未集成。
**分析依据：** [`docs/requirements/001-cloud-edge-diagnostics-analysis.md`](../../docs/requirements/001-cloud-edge-diagnostics-analysis.md)
**前置门槛：** 未决决策 D-001 至 D-014 经用户确认并转写到批准的实现计划前，不得实现业务代码。

## 1. 目标与范围

构建可在受控本地开发环境验证的云边协同设备诊断管理主链路：三类感知终端只向边端发送心跳和检测文件；边端接入、保存、标准化、模拟分析并缓存上报；云端持久化、判断边端离线、支持查询；Web 展示节点和诊断结果。所有链路均独立保留 `assetId`、`terminalId`、`edgeId`、`eventId`。

## 2. 用户故事

### US-001 终端采集与上报（优先级 P1）

作为现场运维人员，我要配置红外、声纹、局放三台终端，让它们按规定周期生成本地测试文件、每 10 秒向其所属边端心跳，并只将文件和元数据发送给边端，以确保终端不绕过边端链路。

**验收场景**

1. 给定已配置的红外、声纹、局放终端，当运行模拟器时，则它们分别每 5、20、60 秒生成检测事件，且心跳间隔为 10 秒。
2. 给定一次采集，当终端上传文件和元数据时，则载荷包含并分别表达 `assetId`、`terminalId`、`edgeId`、`eventId`、模态和采集时间。
3. 给定任一终端，当其发送业务载荷时，则目标仅是配置的边端地址，而非云端地址。

### US-002 边端接入、分析和缓冲（优先级 P1）

作为站内运维者，我要让边端接收终端心跳/文件，校验并安全保存文件，执行模拟诊断，再持久缓存待上报结果，使现场采集可在云端暂不可达时继续被处理。

**验收场景**

1. 给定有效文件和元数据，当边端接收上传时，则记录终端最后在线时间和状态，在受控目录保存文件，并产生规范化记录。
2. 给定有效保存文件，当模拟算法成功时，则结果含缺陷类型、置信度、严重等级以及原始四个 ID。
3. 给定缺少必填 ID、模态不合法、文件类型/大小不合法或不安全路径，当边端接收上传时，则拒绝该请求、不保存不可信文件，并返回明确校验错误。
4. 给定算法输入无效或算法异常，当执行分析时，则记录关联 ID 的错误，且不把虚构诊断结果标为成功。
5. 给定云端不可用，当边端产生诊断结果时，则本地待上报缓存保留该事件，待后续上报重试。

### US-003 边端状态与可靠上报（优先级 P1）

作为云端管理员，我要接收每个边端的资源状态和诊断结果，并能在重试时避免重复事件，以获得可信的节点和诊断数据。

**验收场景**

1. 给定边端状态上报，当云端接收它时，则保存 `edgeId`、站点、CPU、内存、GPU/NPU、上报/接收时间。
2. 给定缓存诊断事件，当上报成功时，则云端保存完整字段，边端只在成功确认后把该事件标为已发送。
3. 给定同一 `eventId` 的重复上报，当云端接收时，则返回幂等成功或等价结果，且仅有一条业务诊断记录。
4. 给定一次失败上报，当网络恢复时，则边端按已确认的重试策略补报，且四个 ID 与第一次上报一致。

### US-004 云端关系管理、离线判断与查询（优先级 P1）

作为云端管理员，我要保存设备、终端、边端关系，按规则判断边端离线，并按多个维度查询诊断结果，以便追溯现场数据。

**验收场景**

1. 给定边端的最后成功接收心跳，当云端当前接收时间超过其 60 秒时，则该 `edgeId` 显示为离线；60 秒内显示为在线。
2. 给定诊断结果，当存储时，则保存 `assetId`、`terminalId`、`edgeId`、`eventId`、模态、缺陷类型、置信度、严重等级、发生时间和证据文件地址。
3. 给定一个或多个 `assetId`、`terminalId`、`edgeId`、模态和时间范围条件，当调用查询接口时，则结果仅符合全部已给条件，并仍返回完整关联 ID。
4. 给定没有匹配记录，当查询时，则返回可区分于错误的空集合/分页结果。

### US-005 Web 管理与证据查看（优先级 P1）

作为管理端使用者，我要查看边端状态、诊断结果列表和诊断详情并筛选，使我能定位异常设备及其证据地址。

**验收场景**

1. 边端节点列表显示边端编号、所属站点、在线状态和最后在线时间。
2. 诊断列表显示设备、终端、边端、模态、缺陷类型、置信度、严重等级和发生时间，并可以按设备、边端、模态筛选。
3. 诊断详情显示完整诊断字段和证据文件地址。
4. 列表和详情在加载、空数据、云端 API 错误时呈现清晰状态；Web 不直接读取数据库。

## 3. 需求可追踪性

| 编号 | 关联用户故事 | 计划章节 | 实现任务 | 验证项 | 验收标准摘要 |
| --- | --- | --- | --- | --- | --- |
| FR-001 | US-001 | 2、3.2、7 | T-003、T-007 | VT-001 | 三台可配置终端及 5/20/60 秒周期。 |
| FR-002 | US-001、US-002 | 2、3.2、7 | T-003、T-004、T-007 | VT-001、VT-002 | 10 秒心跳，边端记录状态。 |
| FR-003 | US-001 | 2、4、7 | T-002、T-003、T-007 | VT-001、VT-003 | 四个 ID 载荷且无终端→云端业务请求。 |
| FR-004 | US-002 | 2、3.3、5、7 | T-002、T-004、T-007 | VT-002、VT-003 | 校验、受控保存、标准化、拒绝非法输入。 |
| FR-005 | US-002 | 2、3.3、5、7 | T-004、T-007 | VT-002 | 模拟算法成功、失败及关联日志。 |
| FR-006 | US-002、US-003 | 2、3.1、5、7 | T-002、T-004、T-007 | VT-002、VT-004 | outbox 状态可观察且不丢未确认事件。 |
| FR-007 | US-003 | 2、3.2、4、5、7 | T-002、T-004、T-005、T-007 | VT-004、VT-005 | 边端状态和诊断可按批准策略上报/重试。 |
| FR-008 | US-004、US-005 | 2、3.2、7 | T-005、T-006、T-007 | VT-005、VT-006 | 云端按接收时间执行 60 秒离线判定。 |
| FR-009 | US-003、US-004 | 3.1、4、5、7 | T-002、T-004、T-005、T-007 | VT-004、VT-005 | 相同 `eventId` 不重复持久化。 |
| FR-010 | US-004、US-005 | 4、6、7 | T-002、T-005、T-006、T-007 | VT-005、VT-006 | 组合筛选仍返回完整关联 ID。 |
| FR-011 | US-005 | 6、7 | T-006、T-007 | VT-006 | 三个 Web 视图和规定 UI 状态。 |
| FR-012 | US-002 至 US-005 | 4、5、7 | T-002、T-004、T-005、T-006、T-007 | VT-002、VT-005、VT-006 | 一致错误响应、无敏感日志及云端自动化测试。 |
| NFR-001 | 全部 | 2、7、8 | T-001、T-007、T-008 | VT-007 | 受控本地环境可启动，且无真实硬件/模型依赖。 |
| NFR-002 | 全部 | 4、7 | T-002、T-007 | VT-003 | 契约明确类型、必填、枚举、时间和失败响应。 |
| NFR-003 | US-002 至 US-004 | 5、7 | T-004、T-005、T-007、T-008 | VT-002、VT-005、VT-007 | 结构化日志含关联 ID 且不泄露敏感信息。 |
| NFR-004 | US-002 | 3.3、5、7 | T-002、T-004、T-007 | VT-002、VT-003 | 文件安全边界和受控存储。 |
| NFR-005 | US-003 | 3.1、5、7 | T-004、T-005、T-007 | VT-004、VT-005 | outbox 与幂等保障重试安全。 |
| NFR-006 | US-005 | 6、7 | T-006、T-007 | VT-006 | Web 的加载、空、筛选失败/API 错误状态。 |
| NFR-007 | 全部 | 7、8 | T-002 至 T-008 | VT-001 至 VT-007 | TDD、分层测试和全量质量门禁。 |
| NFR-008 | 全部 | 2、8 | T-001、T-007、T-008 | VT-007 | 范围、降级和未纳入能力有明确记录。 |

## 4. 业务规则

1. 云端对边端离线的唯一强制阈值是：自该边端最后一次成功心跳接收时间起 **超过 60 秒**即离线。
2. `eventId` 表示一次采集/诊断事件，在终端、边端缓存、云端 API 和云端数据模型中保持相同值；不使用“每次重试的新 ID”。
3. 文件成功保存和元数据标准化是调用算法的前置条件；算法只读取边端保存后的受控路径。
4. 云端查询和 Web 返回应保留四个 ID，即使调用方仅按其中一个过滤。
5. 上报失败不可删除待上报事件；重复交付必须由云端幂等处理。

## 5. 非功能需求

- 时间字段采用 ISO-8601 UTC；云端以其接收时间而非终端上报时间做 60 秒离线计算。
- 文件上传使用受控存储名与目录，限制类型和大小，拒绝客户端给出的路径；日志不输出机密、令牌或文件正文。
- 接口有一致的成功、校验失败、未找到、依赖不可用和幂等重放语义。
- 所有新增可观察行为先写失败测试；跨服务消息以 schema/契约测试验证四个 ID、必填字段和失败响应。
- 首期先验证一个端到端诊断事件，再在相同批准契约下复用至另两种模态。

## 6. 范围外

- 真实采集硬件协议、真实红外/声纹/局放文件解析和真实深度学习模型。
- 终端直接调用云端，云端直接读取边端本地文件系统或数据库。
- 多站点高可用、集群消息队列、生产灾备、自动伸缩、完整运维平台。
- 未确认的生产级 IAM、多租户、审计合规、数据生命周期策略和外网部署。
- UI 视觉精修；首期要求信息完整与可用，而非设计系统。

## 7. 未决决策与阻断条件

本规格不静默选定通信、技术栈细节、存储、认证、文件存储、重试或分页策略。它们逐项记录在分析文档 D-001 至 D-014，并附推荐值。以下条件未确认时，规格不可从 Draft 进入可实现状态：

1. D-001/D-002：终端→边端与边端→云端协议和错误/确认语义。
2. D-003 至 D-007：边端技术、数据库迁移、证据文件的存储/可访问方案。
3. D-008：本地开发与目标环境的认证边界。
4. D-009 至 D-012：`eventId` 生成、幂等、outbox 重试、失败事件和保留策略。
5. D-013/D-014：分页/时间口径、资源指标采样口径。

分析文档第 7 节逐项列出用户确认要求、非批准推荐值和受影响任务；确认后才可更新本规格状态，并将已选值固定在 `plan.md` 或 ADR。
